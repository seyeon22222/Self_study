#include <iostream>
using namespace std;

int k, n;
int len[10001];

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL), cout.tie(NULL);
	cin >> k >> n;
	for (int i = 0; i < k; ++i)
		cin >> len[i];
	
}